# Hello World plugin

This sample plugin demonstrates:

- A minimal `FeaturePack` manifest
- A single route (`GET /hello`)
- Plugin-local Jinja template rendering

Key file: `plugins/hello_world/__init__.py`.

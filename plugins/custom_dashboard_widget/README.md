# Custom Dashboard Widget plugin

This sample plugin demonstrates:

- Returning plugin JSON from `GET /api/plugins/dashboard-widget`
- Calling existing repository code (`app.repositories.companies`)

Use it as a template for plugin-provided API endpoints.
